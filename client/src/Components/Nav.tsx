import React from 'react'
import SectionTitle from './SectionTitle'

const Nav = () => {
  return (
    <nav className='NavBar'>
        Loop:
        <SectionTitle/>
    </nav>
  )
}

export default Nav